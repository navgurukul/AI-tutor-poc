Place the Piper voice model here:

- `en_US-amy-medium.onnx`
- `en_US-amy-medium.json`

Download from https://rhasspy.github.io/piper-samples/ (referenced in the `react-sts-hooks` package docs). These files are not committed to the repo — add them locally before running the tutor screen.
