# Python 3.9+ compatible. Everything here is installed once; the app itself
# runs fully offline against a local Ollama daemon.
fastapi==0.115.6
uvicorn[standard]==0.32.1
httpx==0.28.1
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1

# --- Retrieval (RAG) ---------------------------------------------------
# pypdf is pure Python, so there is no wheel to miss on any target machine.
pypdf==6.16.2
# sqlite-vec ships the SQLite extension the vector search runs on. It needs a
# Python built with extension loading; macOS's system 3.9 is not one, so the
# backend now expects 3.11+ (Windows: winget install Python.Python.3.12).
sqlite-vec==0.1.9
# FastAPI needs this to parse the PDF upload on the setup page.
python-multipart==0.0.20
