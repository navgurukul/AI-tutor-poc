1.How webspeech works offline?
2.Prepare distribuable


Research:
What the first-run installer actually does
    SpeechRecognition.install({ langs: ["hi-IN"] }) downloads a SODA language pack to the Chrome installation (not the profile), then processLocally = true on the recognizer keeps audio on the machine. After that one download, Hindi STT works with no network.
=>hi-IN not available offline

Ollama unloads an idle model after 5 minutes by default, and reloading
    # this one costs ~2s -- a student who pauses between questions would pay it
    # every time. Accepts Ollama's own forms: a number of seconds ("-1" never
    # unloads, "0" unloads immediately) or a duration ("30m", "1h").

Build the index centrally and ship it as a data file. 